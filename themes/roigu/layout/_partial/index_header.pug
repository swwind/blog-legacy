video#bgvideo(autoplay,muted)
  source(src="/img/cxk.mp4",type="video/mp4")

.blog-title
  p.title
    = config.title
  p.subtitle
    = config.subtitle

.search-box.search-icon#search-box
  form(action="/search/",method="get")
    .placeholder
      = __('search')
    input.color-ccc(type="text",name="s")
